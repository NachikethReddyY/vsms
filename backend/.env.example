PORT=5000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vsms
CORS_ORIGIN=http://localhost:5173

# Cognito
COGNITO_REGION=ap-southeast-1
COGNITO_USER_POOL_ID=ap-southeast-1_example
COGNITO_APP_CLIENT_ID=exampleclientid
COGNITO_APP_CLIENT_SECRET=
# Leave false when staff metadata is stored only in Prisma.
COGNITO_USE_CUSTOM_ATTRIBUTES=false

# Optional development seed owner. Use the same email as your Cognito account
# to give that local user ADMINISTRATOR and REGISTRATION_OFFICER roles.
SEED_STAFF_EMAIL=seed.admin@cryptix.local
SEED_STAFF_NAME=Seed Administrator
SEED_STAFF_EMPLOYEE_NUMBER=SEED-ADMIN-001

# Optional label used by the health/root response
AUTH_PROVIDER=cognito
