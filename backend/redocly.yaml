extends:
  - recommended

rules:
  # /auth/authorize is intentionally redirect-only, and /auth/config-status is
  # intentionally anonymous status-only. Keep the OpenAPI contract accurate
  # rather than inventing unreachable responses to satisfy generic style rules.
  operation-2xx-response: off
  operation-4xx-response: off
