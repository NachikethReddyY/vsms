const express = require("express");
const router = express.Router();

// Add your queue routes here when ready
// Example: router.get("/", (req, res) => res.json({ message: "Queue route works" }));

module.exports = router;